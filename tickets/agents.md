Three agents:

- Prodirek: Product Manager
- Arko: Architect  
- Juna: Junior Engineer

Each agent has a markdown file and slice function in config. Routes are generated dynamically from config, not hardcoded.

VSCode providers register through endpoints to receive slice updates when the graph changes.
