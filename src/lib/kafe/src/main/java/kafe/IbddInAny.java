package kafe;

// Generic interface for BDD input types
public interface IbddInAny<I, S, R> {
    // I: Input type for the test
    // S: Subject type (what's being tested)
    // R: Store/Result type for passing state between steps
    
    // This provides type safety throughout the BDD chain
}
