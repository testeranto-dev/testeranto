---
status: doing
files:
  - src/vscode/types.ts
  - src/vscode/TestTreeItem.ts
  - src/vscode/README.md
  - src/vscode/extension.ts
  - src/vscode/providers/FeaturesTreeDataProvider.ts
  - src/vscode/providers/FeaturesTreeDataProviderUtils.ts
  - src/vscode/providers/FileTreeDataProvider.ts
  - src/vscode/providers/ProcessesTreeDataProvider.ts
  - src/vscode/providers/ResultsTreeDataProvider.ts
  - src/vscode/providers/TestTreeDataProvider.ts
  - src/vscode/TestTreeItem.ts
  - src/vscode/TerminalManager.ts
  - src/vscode/README.md
  - src/vscode/extension.ts
---

see [documentizer](./../../server/features/documentizer.md)

see [testJson.md](./../../features/testsJson.md) for an example of a test result file.

Our vscode extension is pretty ragged. We need to take a critical eye to it.

src/vscode/providers/FeaturesTreeDataProvider.ts needs to be thinned out. Move pure functions to src/vscode/providers/FeaturesTreeDataProviderUtils.ts.

ignore src/vscode/providers/FileTreeDataProvider.ts and keep for posterity

src/vscode/providers/TestTreeDataProvider.ts
src/vscode/providers/ResultsTreeDataProvider.ts
src/vscode/providers/ProcessesTreeDataProvider.ts all need to been thinned out too. folow the utils patten of putting pure funtions in a utils file.

A lot of these files will get trashed in v2.

## functionality

We want testeranto to have it's own space int he activity bar. Right now, it's living under the files section, but we want out own dedicated space.

Our extensions will look similar to the basic files section. the left column will show a tree, and the right side will show file contents.

This element differs from the vanilla file explorerer in several ways:

- It does not show ALL the files in the repo, it shows only the
  -- globbed documentation files
  -- input files for tests
  -- reports and a few other key files TBD

- each test has a button that when clicked opens a terminal tab in vscode

- produce an html page with the src folder, with only the features and test results, but no source code. This is for non-coder stakeholders. It's most important goal is to render a tree congruent with our source code structure, and with that tree, show test results and features, but no iimplementation details. We'll need to be able to check this file into git as the testeranto/reports/index.html, because it will be a static website on github. We need to show this somewhere in the vscode extension.

## Architecture

We need to take very critical eye to how we tie the frontend and backend together. The current solution is sloopy, so we will need to decide how to bridge this gap. I believe we are using files ATM but we have the capacity for both HTTPS and WebSockets.

---

1 Consolidate the VS Code extension's activity bar view
Currently we have multiple providers (TestTreeDataProvider, ResultsTreeDataProvider, ProcessesTreeDataProvider, FeaturesTreeDataProvider) that are likely
registered under different views. According to v2.md, we want a single dedicated activity bar space for Testeranto, not scattered across the Files section.
We should create a unified tree view that combines:
• Documentation files (from documentationGlob)
• Test input files
• Test results
• Docker processes
• HTML report generation
2 Refactor providers to follow the utils pattern
We already started with FeaturesTreeDataProviderUtils, but need to do the same for:
• TestTreeDataProvider → move pure functions to TestTreeDataProviderUtils (partially exists)
• ProcessesTreeDataProvider → move pure functions to ProcessesTreeDataProviderUtils (exists but minimal)
• ResultsTreeDataProvider → could be merged into the unified view
3 Implement the static HTML report generation
The requirement is to produce an HTML page that shows only features and test results (no source code) for non‑coder stakeholders. This should be saved to
testeranto/reports/index.html and be viewable from the extension.
4 Improve the WebSocket/HTTP communication
Currently there's a mix of file watching, HTTP endpoints, and WebSocket messages. We need a clean bridge:
• Server already exposes /~/documentation, /~/configs, /~/processes, /~/inputfiles, /~/outputfiles
• The extension should primarily use HTTP for initial data and WebSocket for real‑time updates (resourceChanged)
• We need to ensure the extension re‑fetches when the server broadcasts changes
5 Thin out the providers as requested
Move all pure data‑fetching and tree‑building logic into utils files, leaving the providers as lightweight orchestrators that:
• Handle VS Code tree data provider interface
• Trigger refreshes
• Manage WebSocket connection state
6 Create a single “Testeranto” activity bar view
We should register one tree view (e.g., testerantoView) that uses a unified provider (maybe TesterantoTreeDataProvider). This provider would show sections:
• Documentation
• Test Inputs
• Test Results
• Docker Processes
• HTML Report (static site)
7 Add the “Open HTML Report” command
Provide a way to generate and open the static report from within the extension.
8 Clean up unused providers
After consolidation, we can deprecate FileTreeDataProvider (as per v2.md) and possibly merge ResultsTreeDataProvider into the unified view.

Next immediate steps (edits):

1 Create a unified TesterantoTreeDataProvider (maybe we already have a skeleton in the added files) that replaces the four separate providers.
2 Update extension.ts to register only this unified view under its own activity bar icon.
3 Enhance FeaturesTreeDataProviderUtils to include functions for building the unified tree sections.
4 Add HTML report generation to the server (or a pure function that the extension can call via HTTP) and expose it via a new endpoint /~/html-report.
5 Ensure the server’s documentation watcher is working and broadcasting updates via resourceChanged("/~/documentation").
6 Refactor the other providers’ pure functions into their respective utils files.

We should also check if the current TesterantoTreeDataProvider.ts (which you added) already covers the unified view. If it does, we can refine it and ensure
it's the one registered in extension.ts.
